---

grafana_pkg: grafana

postgresql_database:
  - name: "grafana"

grafana_ldap_domain_password:

grafana_ldap_host:
grafana_ldap_port:
grafana_ldap_bind_dn:
grafana_ldap_search_filter:
grafana_ldap_base_dns:

grafana_ldap_group_search_filter:
grafana_ldap_group_base_dns:

grafana_ldap_admin_group:
grafana_ldap_editor_group:
gradana_ldap_editor2_group:
